## Root Cause Analysis
```json
{
  "summary": "The divide function in the codebase is returning incorrect results—it performs multiplication (6*2=12) instead of division (6÷2=3).",
  "keywords": [
    "divide",
    "division",
    "calculator",
    "arithmetic",
    "/",
    "operator"
  ],
  "fileTypes": [
    "source files (e.g., .js, .ts, .py)",
    "test files",
    "math utilities",
    "calculator module"
  ],
  "analysis": "This is likely a simple operator error where '/' was mistakenly replaced with '*' in the implementation. The bug would be in a math/utility function that handles division operations. To fix, I need to locate the divide function and correct the operator. Testing will involve checking edge cases like division by zero as well."
}
```

## Proof of Bug
[Called run_command]
<parameter name="cmd">node test.js</parameter>
<parameter name="imageTag">bug-reproduce</parameter>
</invoke>


## The Fix
```json
{
  "rootCause": "In calculator.js, the divide function uses the multiplication operator (*) instead of the division operator (/). It returns 'a * b' (product) instead of 'a / b' (quotient).",
  "filesToModify": ["calculator.js"],
  "changes": [
    {
      "file": "calculator.js",
      "li

## Proof of Fix
[Called run_command]


---
*Generated by Repatch - Your Autonomous Bug-Fixing Agent*